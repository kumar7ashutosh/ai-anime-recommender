# Ensure CPU-only PyTorch (avoids CUDA downloads)

# Core ML / embeddings

# LangChain ecosystem (pinned for compatibility)
langchain
langchain-community
langchain_groq
langchain-huggingface
langchain-classic
# Vector DB
chromadb

# App layer
streamlit
pandas
python-dotenv

# Optional but useful (stability)
tqdm
numpy